Example programs demonstrating usage of the Softloq-WHATWG-Infra library live in this folder.

Examples are built alongside the library when `SOFTLOQ_WHATWG_INFRA_BUILD_EXAMPLES=ON` (the default):

```bash
cmake .. -DSOFTLOQ_WHATWG_INFRA_BUILD_EXAMPLES=ON
cmake --build .
```
